# Pass 2 — serve-sim-cloud-ios-e2e-20260923 (adversarial)

Assume FAIL until proven. Attack Fundamentals and Pass2 hammers first.

| Attack | Result |
|--------|--------|
| Goal / one-path only | **PASS** — single Linux→ngrok→Mac:3399→iPhone 16e; EAS/Namespace deferred, not essay-padded |
| Missing ladder rung | **PASS** — A–E files present; cold/ + warm/ Home↔Settings JPEGs; timestamps in logs 10:32–10:34 PT; status bar 10:33 |
| Chrome-only / wrong layer | **PASS** — JPEGs are Expo Go canvas (Home Hex Globe / Settings Theme+Client 55.0.27), not terminal |
| Identical before/after sold as success | **PASS** — first attempt was identical (already Settings+alert); guide + SUMMARY document dismiss; packaged verify-D = cold Home→Settings (SHA delta true) |
| Undeclared local state | **PASS** — Expo Go version, caffeinate, Duo/:3200 leave-alone, repair-input, alert dismiss coords all declared |
| Tunnel contract incomplete | **PASS** — HTTPS/WSS, preview port, no auth on stream/WS, sleep, URL rotate ERR_NGROK_3200, skip-browser-warning |
| Security footgun silent | **PASS** — prereqs warn unauthenticated control; treat BASE as secret; ephemeral tunnel |
| Timing without numbers | **PASS** — `timing.json` cold Settings 527 ms / warm 503 ms; guide table matches |
| Placeholders undefined | **SOFT** — `UDID`/`PORT`/`BASE` defined; `/path/to/serve-sim-docs/tools` is conventional clone path, not session secret — not SB |
| “Proven twice” without pack | **PASS** — claim = first + re-run; this pack proves re-run with **two** visual confirms (cold+warm); first run cited outside |
| Claim: Linux→iPhone 16e Home→Settings via ngrok + binary WS, 2026-09-23 PT ×2 | **PASS** — commands.log + topology + cold/warm JPEGs + verify-E; client uses binary WS touch tag 0x03 |

**Ship-blocks found:** none

**Pass2:** Excellent
